import { metadata } from './metadata'

export { metadata }

export default function LoanInterestLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <>
      {children}
    </>
  );
}
